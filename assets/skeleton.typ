#import "../assets/template.typ": *

#tool-sheet("TOOL NAME")[
  == Core Commands
  #commands-table((
    ([`command --flag`], [what it does, one line]),
    ([`command --flag`], [what it does, one line]),
  ))

  == Workflows
  + First step of a multi-step task you actually do with this tool.
  + Second step.
  + Third step.

  == Gotchas
  - Non-obvious behavior #1 — be specific, this is the section that earns its place.
  - Non-obvious behavior #2.
]  
